#include "main.h"

/**
*puts2 -  prints every other character of a string
*@str: input string variable
*Return: returns zero by default unless determined
*/
void puts2(char *str)
{
int i = 0;
{
if ((i % 2) == 0)
  i++;
_putchar(str[i]);
else
continue;
}
_puthcar('\n');
}
