#include "main.h"
/**
* main - main serves as the starting point for program execution
*Return: returns zero by default
*/
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar (ch)
putchar ('\n');
return (0);
}
